import SearchItem from ".";

export default {
    title: "molecules/SearchItem",
    component: SearchItem,
};
const Template = (args) => <SearchItem {...args} />;
export const PrimarySearch = Template.bind({});

PrimarySearch.args = {
    
    
};